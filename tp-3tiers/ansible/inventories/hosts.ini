# Inventaire statique — utilisé à l'étape 1 (avant le premier terraform apply)
# Après terraform apply, le fichier generated.ini prend le relais (ansible.cfg)

[local]
localhost ansible_connection=local

[docker_hosts]
localhost ansible_connection=local

[all:vars]
ansible_python_interpreter=/usr/bin/python3
